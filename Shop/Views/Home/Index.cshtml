@model HomeViewModel

@await Html.PartialAsync("Carousel")
<h2>Preferred products of the week</h2>
<h4>Clients preferences - just for you</h4>

<div class="row">
    @foreach(Product product in Model.PreferredProducts)
    {
        @Html.Partial("ProductSummary", product)
    }
</div>